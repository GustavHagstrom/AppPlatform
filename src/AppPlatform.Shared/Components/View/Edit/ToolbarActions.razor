@inject IStringLocalizer<ToolbarActions> Localizer

<SectionEditor/>

@* <FormatEditor SectionTitle="@Localizer["Kolumn"]" Format="ColumnFormat"/>
<FormatEditor SectionTitle="@Localizer["Rad"]" Format="RowFormat" />
<FormatEditor SectionTitle="@Localizer["Ändrad rad"]" Format="ChangedRowFormat" /> *@

@code{
    [Parameter] public IFormat? ColumnFormat { get; set; }
    [Parameter] public IFormat? RowFormat { get; set; }
    [Parameter] public IFormat? ChangedRowFormat { get; set; }
    [CascadingParameter] public ViewPresenter? ViewPresenter { get; set; }

    protected override void OnParametersSet()
    {
        base.OnParametersSet();
    }
}
