<div class="d-flex flex-column gap-2">
    @ChildContent
</div>

@code{
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public string? Class { get; set; }

    string ClassNames => $"d-flex gap-2 {Class ?? string.Empty}";
}